const PUBLIC_URL = '/public';
const AUTH_URL = '/auth';
const API_URL = '/api';
const TOKEN = 'id_token';

const PATHS = {
    HOME: '/',
    LOGIN: '/login',
    REGISTER: '/register',
};

export {
    AUTH_URL,
    PUBLIC_URL,
    API_URL,
    TOKEN,
    PATHS,
};
